{
  "name": "@formsync/extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome MV3 bridge: page postMessage to local FormSync MCP host.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
